{
  "name": "tokenshelf",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fmt": "npm --prefix app run fmt && npm --prefix cli run fmt",
    "fmt:check": "npm --prefix app run fmt:check && npm --prefix cli run fmt:check",
    "test": "npm --prefix app test && npm --prefix cli test",
    "test:app": "npm --prefix app test",
    "test:cli": "npm --prefix cli test",
    "build:cli": "npm --prefix cli run build"
  }
}
