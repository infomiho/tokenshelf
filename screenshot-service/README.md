# Screenshot service

Start the local stack:

```sh
docker compose -f compose.screenshots.yml up --build
```

RustFS is available at `http://localhost:9000`, its console at `http://localhost:9001`, and the screenshot service at `http://localhost:4100`.
